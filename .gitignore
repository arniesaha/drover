# Environments
.env
.venv
env/
venv/
ENV/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg

# OS
.DS_Store

# GCP Key
gcp-key.json

# Local lakehouse data (too large for git)
local_backup/
parquet/
*.duckdb
*.duckdb-wal
*.duckdb-shm

# Drover runtime state (per-host home dir, not in repo; .nexus is legacy)
.drover/
.nexus/

# IDE
.idea/
.vscode/
.claude/
.swiftpm/

# Local instructions for coding agents. Deliberately untracked: AGENTS.md
# records where internal planning documents actually go, which is private
# workflow detail this public repository must not publish.
AGENTS.md

# Local Swift/Xcode build caches
apps/drover/.derivedData*/
apps/drover/.xcode-home/
apps/drover/DroverKit/.build/
apps/drover/DroverKit/.clang-cache/
apps/drover/DroverKit/.swift-cache/

# Local git worktrees
.worktrees/

# Private planning belongs in the private roadmap repository.
docs/internal/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
.codex
